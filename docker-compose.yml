version: '3.9'

services:
  blackout-simulator:
    build: .
    environment:
      - RUST_LOG=debug
    ports:
      - "3030:3030"
    volumes:
      - .:/app
    command: ["/usr/local/bin/blackout-engine"]
    networks:
      - blackout-net

networks:
  blackout-net:
    driver: bridge
